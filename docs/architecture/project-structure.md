# 项目结构说明

当前仓库采用六大核心板块组织代码：

- `frontend/`：桌面端展示。
- `backend/`：后端接口与任务编排。
- `algorithms/`：模型训练、推理和配置。
- `data/`：原始数据、导出数据和数据库。
- `deploy/`：Docker、脚本和服务化配置。
- `docs/`：架构、开发和部署文档。

该结构适合多人协作下的模块并行开发，也便于逐步从骨架迭代到可交付系统。
