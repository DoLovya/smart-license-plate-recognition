# 数据模块说明

## 目录约定

- `raw/`：原始图片、视频与临时采集数据。
- `interim/`：清洗、裁剪、增强等阶段产生的中间产物。
- `processed/`：训练或评估前的标准化数据。
- `exports/`：识别结果导出文件，例如 `CSV`、`JSON`。
- `sqlite/`：SQLite 初始化脚本与数据库文件。

## 使用建议

- 原始数据、导出结果和数据库文件默认不纳入版本管理。
- 团队协作时通过脚本或对象存储同步数据，不直接提交大体积文件。
